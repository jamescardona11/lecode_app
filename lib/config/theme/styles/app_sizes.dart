import 'package:flutter/material.dart';

class AppSizes {
  const AppSizes._();

  //app
  static const double sizeHeaderBar = 65;

  // radius
  static const Radius radius16 = Radius.circular(10);

  // margins
  static const double defPadding = 16;
}
